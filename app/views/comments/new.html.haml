%h1=t '.comment_new'

- semantic_form_for @comment, :url => talk_comments_path do |f|
  = f.semantic_errors
  - f.inputs do
    = f.input :message, :label => t('.message')
  - f.buttons do
    = f.commit_button :button_html => { :value => t('.add') }