%h1 
  Список событий
  = link_to image_tag("ical.png"), events_url(:ics)

- if @events.empty? 
  %p К сожалению, в ближайшее время событий не предвидится.
- else    
  - @events.each do |event| 
    .event_block
      .started_at
        %span= event.started_at.to_formatted_s(:event)
      .linked_name= link_to event.name, event_path(event)
      .place
        %img{:src=>"images/tiny_pin.png"}
        →
        = event.place
      .vk_event
        %img{:src=>"images/vkontakte.gif"}
        →
        = vkontakte_event_url event.vkontakte_event_id
      .retweet= link_to image_tag("retweet-over.png"), URI.escape(['http://twitter.com/home?status=' + event.started_at.to_s(:event), event.name, url_for(:action => 'show', :id => event.id, :only_path => false)].join(' '))
    .twitter
      %script{:src => "http://widgets.twimg.com/j/2/widget.js"}
      %script{:src => "/javascripts/twitter.js"}